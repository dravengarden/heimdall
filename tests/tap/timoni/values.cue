// User-overridable defaults. `timoni apply --values <file>` merges
// any file matching the same shape on top of these values. The schema
// + defaults live in templates/config.cue; this file exists because
// timoni requires it at the module root.
package main

values: {
}
