{
  "lock": "deno.lock",
  "nodeModulesDir": "auto",
  "tasks": {
    "dev": "deno run -A npm:vite",
    "build": "deno run -A npm:vite build",
    "preview": "deno run -A npm:vite preview",
    "typecheck": "deno run -A npm:typescript/tsc --noEmit",
    "lint": "deno run -A npm:oxlint src"
  }
}
