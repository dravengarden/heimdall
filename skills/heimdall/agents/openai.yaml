interface:
  display_name: "Heimdall"
  short_description: "Operate the command-scoped SOCKS5 proxy wrapper"
  default_prompt: "Use $heimdall to validate my config and run this command through the selected proxy."
