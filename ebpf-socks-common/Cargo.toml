[package]
name = "ebpf-socks-common"
version = "0.1.0"
edition = "2021"

# no_std for eBPF side; "user" enables std + aya::Pod
[features]
default = ["user"]
user = ["aya"]

[dependencies]
aya = { version = "0.13", optional = true }
